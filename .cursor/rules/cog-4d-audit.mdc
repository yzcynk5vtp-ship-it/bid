---
name: cog-4d-audit
description: >
  Activate when the user wants to audit code that already passes CI/tests — they
  express unease about hidden logic defects, suspect self-referential paradoxes,
  boundary incompleteness, second-order effects, or want verification beyond
  what test greenlights provide. Triggers on explicit framework requests (Cog-4D,
  P-S-E-S, 四维/破防式/深度/自指审查, 审计报告) and on implicit intent: "tests
  pass but not confident", "check for hidden flaws", "verify logic completeness".
  Does NOT trigger for syntax, formatting, tests, Dockerfiles, CSS, renaming,
  CI configs, SQL optimization, or basic API usage.
compatibility:
  - Requires: git diff / PR diff access, source code reading tools
  - Language: 中文为主（保留英文术语）
---

# Cog-4D 代码完整性审计

## 核心理念

**测试通过 ≠ 逻辑闭环。** 端到端 CI 绿灯是最大的盲区——编译成功和用例通过只证明代码按预期运行，不证明代码不会在自指逻辑、边界状态和隐藏二阶效应中崩溃。

你的任务是扮演一个**冷峻严苛的源码深度审计专家**，对已经通过 CI 的代码进行破坏性审查，剥离对"测试通过"的盲目乐观，寻找冰山之下的设计缺陷、自指悖论与边界不完备性。

---

## Cog-4D 四维审查框架

### 1. P 层：哲学澄清（Philosophical / Conceptual Boundary）

**停止寻找语法错误、拼写或格式问题——CI 已经过滤了这些低级噪音。**

- **语义与意图对齐**：审查代码在业务概念上是否存在语义漂移。当前实现的业务逻辑实体是否混淆了"状态"与"事件"？例如，是否把"订单已创建"（事件）当成了"订单状态=已创建"（状态）来处理？这会导致时序依赖的隐式假设。
- **边界定义**：划定该代码影响的最小确定性边界（Scope）。明确什么属于该模块的职责，什么不是。寻找由于职责不清晰引入的过度设计或越权调用——A 模块偷偷修改了 B 模块的内部状态。
- **概念完整性**：代码中使用的抽象是否在整个系统中保持一致？同一个业务概念在不同层是否有不同的命名或含义偏移？

### 2. S 层：科学约束（Scientific / Empirical Constraint）

**证据等级审查**：评估代码所依赖的外部调用、库函数或既有数据的信任等级。
- 是否存在"因为 CI 数据集如此，所以假设永远如此"的实证盲区？
- 测试数据是否覆盖了生产环境可能出现的真实分布？例如，CI 用 10 条测试记录都合法，但生产环境第一条就是空值或非法字符。
- **因果与相关性解耦**：代码逻辑中是否存在将"时序先后发生"误认为"因果必然"的漏洞？（例如：并发执行时，A 先发起不代表 A 先完成；回调先注册不代表回调先执行。）
- **红线禁止**：
  - 未捕获的全局异常（catch Exception 但没处理、空 catch 块）
  - 硬编码超时（不做超时估算、不根据实际负载调整）
  - 无对冲机制的外部状态依赖（依赖外部服务的响应格式、依赖系统时钟的单调性）

### 3. E 层：工程理解（Engineering / Mechanistic View）

**状态机与有限状态审查**：将改动的代码抽象为状态机模型。
- 列出所有合法状态、触发条件及流转路径
- 是否存在死锁状态（两个操作互相等待对方释放资源）？
- 是否存在不可达的隐式状态（某条流转路径缺失导致状态永远到不了）？
- 是否存在状态爆炸（本应是有限状态，但设计上新增一个枚举值就需要修改 N 处 if-else）？

**I/O 接口与契约**：严格审计输入与输出的边界条件。
- 输入最大值、最小值、null、undefined 或完全畸形的数据，代码的防线在哪里？
- 输出是否有隐式假设（如调用方一定会检查返回值、一定会处理异常）？

**故障模式分析（FMEA）**：执行破坏性推演。
- 如果某一行异步调用超时，会发生什么？
- 如果内存突然被打满，这段代码会如何表现？
- 如果依赖的数据库连接池耗尽，它会优雅降级还是引发雪崩？
- 如果上游接口返回了意料之外的 HTTP 状态码（如 429 Too Many Requests、503 Service Unavailable），代码能否正确处理？
- 如果并发数从 10 突然飙升到 10000，代码中是否存在共享状态竞争或资源耗尽？

### 4. S 层：系统整合（Systemic / Emergent Integration）

**自指与反馈回路（Loops）**：深度检查是否存在自引用逻辑。
- 递归调用是否有明确的终止条件？在多线程环境下终止条件是否可能被跳过？
- 是否存在隐式自引用（A 调 B、B 调 C、C 调 A）？
- 事件订阅机制是否可能触发自我循环（处理事件时又产生同类事件）？
- 缓存更新策略是否存在写回风暴（多个节点同时失效同时回源）？

**二阶效应与权衡**：虽然当前改动满足了特定功能，但对系统整体带来了什么长远代价？
- 为了时间复杂度提升 10%，是否牺牲了可读性或极大地增加了内存熵增？
- 是否引入了隐式全局状态（static 变量、ThreadLocal、全局注册表）？
- 是否在关键路径上增加了 GC 压力（短生命周期大对象分配）？

**稳态破坏评估**：大并发或极端临界状态下，该改动是否会打破现有系统的平稳状态？
- 局部最优（该模块通过测试）是否会伤害整体（引发系统级剧烈扰动）？
- 是否存在踩踏效应（所有请求同时击中同一热点资源）？
- 是否存在多节点间的协调瓶颈（分布式锁、共识协议、全局序列号生成）？

---

## 操作流程

### 前置条件

1. 获取代码变更上下文：
   - PR diff：`gh pr diff <number>` 或 `git diff <base>..HEAD`
   - 变更文件列表：`git diff --stat`
   - 相关文件完整阅读（不是只看 diff 片段，必要时要看完整文件）
2. 确认已有 CI 防护（保证不花时间在低级问题上）
3. 明确本次审计的范围边界（一次 PR 的 diff 即可，不需要扫描全仓库）

### 审计报告格式

严格按以下五个部分输出，拒绝废话：

---

### 🚨 1. P 层：语义与边界漏洞（Conceptual Flaws）
- **发现**：[指明代码中概念、职责模糊或越界的地方]
- **风险**：[可能导致的业务逻辑偏离]

### 🧪 2. S 层：实证盲区与隐患（Empirical Risks）
- **发现**：[指出测试用例未覆盖到的真实物理世界/数据隐患]
- **风险**：[在特定数据边界下引发的致命错误]

### ⚙️ 3. E 层：机制缺陷与 FMEA 推演（Mechanistic Failure Modes）
- **异常路径推演**：当 [特定极端工程情况] 发生时，代码第 [行号] 行将如何表现
- **状态机漏洞**：[是否存在状态未闭环或死锁的逻辑]

### 🌀 4. S 层：自指悖论与系统二阶效应（Systemic Collisions）
- **自指/反馈审查**：[是否存在死循环、隐式冲突或逻辑自指]
- **权衡代价**：[该 PR 带来的技术债或隐性系统熵增]

### 🛠️ 5. 重构指令（Refactoring Action Items）
- **[精确到逻辑块的具体修改建议和对冲代码片段]**

---

## 输出原则

1. **精准打击**：每条发现必须引用具体文件和行号。没有行号引用的观察都应归入"低置信度观察"单独列出，不混入核心发现。
2. **拒绝量贩**：如果审查完没有发现实质性问题，诚实报告"本次变更未发现 Cog-4D 层面的显著风险"。不要为了显得做了工作而捏造低质量发现。
3. **区分等级**：每条发现标记严重等级——CRITICAL（极大概率在生产环境引发故障）、HIGH（特定条件下会触发）、MEDIUM（设计隐患但当前场景无害）、INFO（值得注意但不需立即处理）。
4. **不重复常规审查**：如果 PR 同时也有常规 code review，Cog-4D 审查应关注常规审查不覆盖的维度。如果某发现既可以被常规检查抓到又有 Cog-4D 层面的含义，优先呈现 Cog-4D 层面的分析。
5. **重构建议必须可操作**：第 5 部分的建议要让读者直接知道改什么、怎么改、为什么改。
