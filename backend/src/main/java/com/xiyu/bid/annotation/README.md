# Annotation 模块

> 一旦我所属的文件夹有所变化，请更新我。

## 职责说明
注解模块定义面向 AOP 的自定义注解，用于操作记录、增强和横切关注点标记。
该目录本身只提供标记能力，不直接承载业务逻辑。
主要由切面模块读取并执行后续处理。

## 边界清单

| 文件 | 地位 | 功能 |
|------|------|------|
| `Auditable.java` | Annotation | 操作日志注解，内部沿用 `Auditable` 命名兼容既有调用 |
