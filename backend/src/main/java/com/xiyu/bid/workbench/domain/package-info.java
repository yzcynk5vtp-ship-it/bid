/**
 * Workbench domain pure core policies.
 * All classes in this package must follow FP-Java Profile:
 * - No framework dependencies
 * - Immutable records preferred
 * - No side effects (no DB, file, network, logging)
 */
package com.xiyu.bid.workbench.domain;
