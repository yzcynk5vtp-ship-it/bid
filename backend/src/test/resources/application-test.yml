spring:
  application:
    name: xiyu-bid-poc-test

  datasource:
    url: jdbc:h2:mem:testdb-${random.uuid};DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        generate_statistics: true

  flyway:
    enabled: false

  data:
    redis:
      enabled: false

  cache:
    type: simple

  task:
    scheduling:
      enabled: false

  jackson:
    default-property-inclusion: non_null

jwt:
  secret: test-secret-key-for-jwt-token-generation-and-validation
  expiration: 3600000
  refresh-expiration: 7200000

cors:
  allowed-origins: http://localhost:1314,http://127.0.0.1:1314,http://localhost:1323,http://127.0.0.1:1323

platform:
  account:
    encryption-key: test-platform-encryption-key-2026

rate:
  limit:
    default:
      max: 999999
      window-seconds: 1
    api-key:
      max: 999999
      window-seconds: 1
    login:
      max-attempts: 999999
      window-minutes: 1

app:
  tender-processing:
    worker-enabled: false

logging:
  level:
    com.xiyu: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.stat: ERROR
    org.hibernate.engine.internal.StatisticalLoggingSessionEventListener: ERROR
