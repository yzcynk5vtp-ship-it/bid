# 本周开发总结（2026-05-22 ~ 2026-05-29）

## 一、本周计划回顾

上周制定了两项开发计划：

| 计划 | 分支 | 状态 |
|------|------|------|
| 标讯创建/详情表单字段修正 | `codex/bidding-create` | 已合并（上周五） |
| 项目列表 UI 精细化 | `012-project-list-ui-refinement` | 已合并（本周五） |

---

## 二、计划一：标讯创建/详情表单字段修正（`codex/bidding-create`）

**输入**：5项表单字段修正需求（specs/004-bidding-form-fix/spec.md）
**输出**：已合并至 main（PR #484）

### 完成情况

| 用户故事 | 内容 | 状态 |
|---------|------|------|
| US1 | 删除"预算金额"字段（创建表单 + 详情页） | ✅ |
| US2 | "标讯标题" → "项目名称"（创建表单 + 详情页） | ✅ |
| US3 | 报名截止/开标时间格式统一为 `yyyy-MM-dd HH:mm` | ✅ |
| US4 | 客户类型选项从3项扩充为5项 | ✅ |
| US5 | 来源平台字段在创建表单中隐藏 | ✅ |

### 涉及文件（6个）
- `constants.js` — 常量修正（budget字段、校验规则）
- `ManualTenderDialog.vue` — 删除预算金额、标签重命名、时间格式
- `DetailPage.vue` — 删除预算金额行、标签重命名
- `helpers.js` — buildManualTenderPayload 移除 budget
- `constants.spec.js` — 单元测试同步
- `ManualTenderDialog.spec.js` — 单元测试同步

**注意**：后端 `budget` 列保留在数据库中，前端仅移除展示层逻辑。

---

## 三、计划二：项目列表 UI 精细化（`012-project-list-ui-refinement`）

**输入**：项目列表 UI 标签统一、列调整、筛选区重排需求（specs/012-project-list-ui-refinement/spec.md）
**输出**：已合并至 main（PR #486）

### 完成情况

| 用户故事 | 内容 | 状态 |
|---------|------|------|
| US1（P1） | 业主单位 → 招标主体；负责人部门 → 项目负责人部门 | ✅ |
| US2（P1） | 投标状态 → 项目状态；移除中标状态列 | ✅ |
| US3（P2） | 筛选区：项目部门负责人 ↔ 投标负责人换位；表格：项目负责人 ↔ 项目部门负责人换位；项目状态移至末列 | ✅ |
| US4（P2） | 日期选择器标签完整显示（CSS 修复） | ✅ |
| US5（P3） | 项目类型筛选器移除"单项目"选项（保留5项：办公/综合/集采/工业品/其他） | ✅ |

### 涉及文件（13个，含测试和文档）
- `List.vue` — 标签重命名、列调整、字段换位、CSS修复
- `useProjectSearch.js` — projectTypeOptions 修正（移除 SINGLE_PROJECT）
- `useProjectColumns.js` — columnOptions 移除中标状态
- `List.spec.js` — 单元测试断言同步
- `InitiationStage.vue` / `InitiationStage.spec.js` — 同步标签更新
- `EvaluationSummaryBar.vue` — 同步标签更新
- 文档（plan.md、research.md、spec.md、requirements.md）

### 专项修复（PR合并后）

| 提交 | 内容 | 性质 |
|------|------|------|
| `fa76681f0` | 修复项目列表负责人筛选器绑定问题（后端填充 leaderName 字段 + 前端改 el-input 文本输入） | Hotfix |

---

## 四、本周工程保障

### CI 稳定性
- 修复 E2E 测试不稳定问题（commercial-main-flow、knowledge-qualification 等5个测试）
- 修复后端正则测试 45 个存量失败归零
- 修复 E2E strict mode 冲突（getByRole 替换文本选择器）
- 放宽 market-timing 断言兼容性
- AI Center E2E 兼容 AI 未配置时的空状态

### 质量门禁
- 两层分支卫生机制（自动清理 + 预防提醒）
- 后端 PR CI 新增 agent-locks 检查，修复 self-merge orphan 问题

### 架构治理
- skills 目录扁平化重构（清理重复文件）
- ClientSDK jar 改为本地 libs 依赖，CI 无需访问私服

---

## 五、遗留与延期

| 项目 | 说明 |
|------|------|
| V1006/V1007 Flyway 迁移 | `V1006` 存储过程幂等化修复（生产安全），`V1007` 继续推进 |
| CRM BaseUrl 多域名路由 | US1+US2 已完成，US3 延期（docs 文档已闭环） |
| 组织架构 SDK 集成 | 文档持续补充，待服务器联调验证 |

---

## 六、产出统计

| 指标 | 数量 |
|------|------|
| 合并 PR | 4 个（#483, #484, #485, #486） |
| 涉及前端文件 | ~15 个 |
| 涉及后端文件 | ~5 个 |
| E2E 测试修复 | ~10 个 |
| 新增/更新文档 | ~8 个 |

---

**撰写日期**：2026-05-29
